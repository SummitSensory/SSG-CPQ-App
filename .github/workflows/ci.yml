name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: app
          POSTGRES_PASSWORD: app
          POSTGRES_DB: app
        ports: ['5432:5432']
        options: >-
          --health-cmd="pg_isready -U app"
          --health-interval=10s --health-timeout=5s --health-retries=5
    env:
      DATABASE_URL: postgresql://app:app@localhost:5432/app?schema=public
      DIRECT_URL: postgresql://app:app@localhost:5432/app?schema=public
      JWT_ACCESS_SECRET: ci-access-secret-xxxxxxxx
      JWT_REFRESH_SECRET: ci-refresh-secret-xxxxxxx
    steps:
      - uses: actions/checkout@v5
      # pnpm/action-setup@v4 still targets Node 20, which GitHub has deprecated and
      # now force-runs on Node 24. Corepack reads `packageManager` from package.json,
      # so it installs the same pinned pnpm with no third-party action involved.
      - run: corepack enable
      - uses: actions/setup-node@v5
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:generate
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm db:migrate:deploy
      - run: pnpm db:migrate:status
      - run: pnpm test
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm build
      - run: pnpm test:e2e
