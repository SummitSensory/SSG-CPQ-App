dist
coverage
playwright-report
pnpm-lock.yaml

# Vendored, minified third-party bundle — see scripts/build-blob-client.mjs.
# Regenerated by that script, not hand-edited, and not valid Prettier input
# once minified.
public/vendor

# The browser app is one 8,000-line file written in a consistent hand-rolled
# style. Letting Prettier at it would rewrite the whole thing on the first
# commit that touches it, burying every real change in reformatting noise and
# making `git blame` useless on the busiest file in the repo. Excluded until
# someone decides to reformat it deliberately, as its own commit.
public/app.js

# The primitives lifted out of app.js under AUD-003. Every body in it is copied
# character-for-character out of the file above, which is the only way a reviewer can
# confirm this was a move and not a rewrite — esc has 780 call sites and td has 301.
# Reformatting it is a fine idea and belongs in its own commit, after this one has
# been proven on every screen.
public/ssg-ui.js

# Same reason, one step further out: the 270 lines of dialog in here moved verbatim out
# of the file above, so the diff reads as the move it is. Prettier would rewrap 38 of
# them on the first commit that touches this file and hide that. Reformat deliberately,
# as its own commit, once the move has been proven.
public/ssg-vendor-parts.js

# And the Catalog screen: 2,451 lines, moved verbatim for the same reason. Reformatting
# it would rewrite nearly the whole file on the first commit that touches it.
public/catalog.js

# Generated or hand-maintained data, not source.
prisma/migrations
src/proposals/adventure-skus.json
prisma/seed-catalog.json
prisma/seed-soar.json
